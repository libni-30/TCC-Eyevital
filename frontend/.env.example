# Variáveis de ambiente para o Vite/React
#
# Renomeie este arquivo para ".env" ou crie ".env.local" e defina os valores.
# Somente chaves que começam com VITE_ ficam disponíveis no código do frontend.

# URL base da API/banco (ex.: backend REST, Supabase, Firebase Functions etc.)
# Desenvolvimento local: use VITE_API_BASE_URL
VITE_API_BASE_URL=

# Vercel: use URL_BASE_API_VITE (configure nas variáveis de ambiente do Vercel)
# URL_BASE_API_VITE=

# Caso seu backend use caminhos diferentes, você pode sobrescrever:
# VITE_AUTH_LOGIN_PATH=/auth/login
# VITE_AUTH_REGISTER_PATH=/auth/register
# VITE_AUTH_ME_PATH=/auth/me
# VITE_AUTH_LOGOUT_PATH=/auth/logout

# Exemplos de outros provedores (descomente se for usar)
# Se usar Supabase para Auth/DB, preencha as duas abaixo e o app usará Supabase automaticamente
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_AUTH_DOMAIN=
# VITE_FIREBASE_PROJECT_ID=
# VITE_FIREBASE_STORAGE_BUCKET=
# VITE_FIREBASE_MESSAGING_SENDER_ID=
# VITE_FIREBASE_APP_ID=
