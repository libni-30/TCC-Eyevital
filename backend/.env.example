## Backend (.env.example)
# Copie para .env e preencha os valores reais (NÃO commitar .env)

# String de conexão completa do Postgres Neon (recomendado usar esta variável)
# Exemplo: DATABASE_URL=postgres://usuario:senha@ep-xxxxx-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=

# (Alternativa legada) Caso queira usar o nome BD_UM já referenciado no código
# BD_UM=

# Segredo para assinar JWTs (gere algo forte, ex: openssl rand -hex 32)
JWT_SECRET=defina-um-segredo-forte-aqui

# Porta do servidor Express (default 3000)
# PORT=3000

# Chave de reset de senha DEV (usada pelo endpoint /auth/dev-reset-password)
# O cliente envia VITE_DEV_RESET_KEY (padrão 'devkey'); o servidor espera DEV_RESET_KEY (padrão 'devkey')
# DEV_RESET_KEY=devkey

# Configurações de SMTP para envio de e-mails de reset
# Exemplo: Mailtrap (recomendado para desenvolvimento)
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_USER=seu_usuario
# SMTP_PASS=seu_token
# SMTP_FROM="Eyevital <no-reply@eyevital.com>"

# Produção (exemplos):
# SendGrid SMTP
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=SG.xxxxxxxx
# SMTP_FROM="EyeVital <no-reply@eyevital.com>"

# Gmail (não recomendado para produção, mas funciona com App Password)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu.email@gmail.com
# SMTP_PASS=senha_de_aplicativo_16_chars
# SMTP_FROM="EyeVital <seu.email@gmail.com>"

# Base URL do frontend para montar links do e-mail (inclui #/ para rotas hash)
# Ex.: FRONTEND_BASE_URL=http://localhost:5173
FRONTEND_BASE_URL=
